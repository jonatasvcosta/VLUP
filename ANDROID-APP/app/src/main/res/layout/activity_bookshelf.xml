<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">
        <android.support.v7.widget.RecyclerView
            android:id="@+id/bookshelf_activity_categories_list"
            android:layout_width="match_parent"
            android:clipToPadding="false"
            android:paddingTop="@dimen/margin_large"
            android:layout_height="match_parent"
            android:background="@color/cyan_lighter"
            android:paddingBottom="@dimen/margin_medium"/>
    </LinearLayout>
    <com.melnykov.fab.FloatingActionButton
        android:id="@+id/fab_random_word_all"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|right"
        android:src="@drawable/ic_fab_random"
        android:layout_marginRight="@dimen/margin_small"
        android:layout_marginBottom="@dimen/margin_small"
        app:fabSize="normal"
        android:clickable="true"
        app:fab_colorNormal="@color/cyan_dark"
        app:fab_colorPressed="@color/cyan_darker"/>
</FrameLayout>
